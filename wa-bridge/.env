# Where to send batches (your FastAPI ingest)
BACKEND_INGEST=http://127.0.0.1:8000/ingest/wa

# Must match the WA_JWT_SECRET in your server/.env
JWT_SECRET=replace-with-your-secret

# Helpful id to allow/track this bridge
BRIDGE_ID=macbook-elad

# Flush batch every N ms or when buffer hits BATCH_SIZE
FLUSH_MS=1500
BATCH_SIZE=200
